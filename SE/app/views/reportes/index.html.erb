<% if signed_in? %>


    <h1>Colegios evaluados</h1>
    <p>Las IE intervenidas pueden así mismo beneficiarse de la intervención integral
       de los siguientes programas, los cuales son ofrecidos a todas las IE de la
       ciudad pero prioritariamente (en algunos casos) a las Escuelas de Calidad.
       Los programas que serán tenidos en cuenta en este estudio son aquellos que
       también podrían afectar los indicadores de resultados, permanencia,
       eficiencia  y clima escolar de las IE intervenidas. Estos son:
    </p>


    <p>&nbsp;</p>

    <h3>Ranking de las instituciones</h3>
    <p>Este es un reporte de las mejores 20 Instituciones Educativas publicas de Medellín, en este reporte
    estan los totales de las variables de Ambiente Escolar, Rendimiento y Progreso, el ranking esta basado
    en la suma de estos tres indicadores.</p>
    <%= link_to "Generar", {:action => "ranking"}, :class => "option_button" %>
    <p>&nbsp;</p>
    <p>&nbsp;</p>
    <p>&nbsp;</p>

    <hr>
    <p>&nbsp;</p>
    <h3>Reporte Individual</h3>
    <p>Este reporte muestra los resultados del estudio de cada institución, seleccione un colegio para obtener
      el resultado, también tiene la opción de descargar el reporte en PDF.

    </p>

    <form name="nombreColegios">
      <select name="colegios" class="option_select " >
        <option id= "1" value>SELECCIONE UN COLEGIO</option>
        <%@colegios.each do |col|	  %>
            <OPTION id="<%= col.dane %>" VALUE="<%= col.nombre %>"><%= col.nombre %></OPTION>
        <% end %>
      </select>
    </form>

    <button  id="button" class="option_button">Generar</button>


    <script>
        function redireccionColegio(){
            var id = nombreColegios.colegios.options[nombreColegios.colegios.selectedIndex].id
            window.location = "reportes/" + id;
        }

        $("button").click(function () {
                    var id = nombreColegios.colegios.options[nombreColegios.colegios.selectedIndex].id
                    if (id === "1"){
                        alert("Seleccione una Institucion Educativa" );
                    }else{

                    window.location = "/reportes/" + id;
                    }
                }
        );
    </script>

<% else %>
      <script type="text/javascript">
          window.location = "/singin"
              ;
    </script>
<% end %>
<div class="ajustar"></div>
